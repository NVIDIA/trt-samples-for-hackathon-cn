# MIG

+ Minimal helper example for using TensorRT in Multi-Instance GPU (MIG) mode.

+ This sample does not build an engine. It detects MIG instances and prints
  practical environment settings for binding a process to one MIG slice.

+ Steps to run.

```bash
python3 main.py
```
