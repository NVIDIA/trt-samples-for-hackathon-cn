version: 1
name: 05-Plugin/CuteDSLPlugin
enabled: true
tags:
  - optional-package   # needs cupy (CUDA-major-matched wheel); skips cleanly if unusable
  - plugin
  - python
timeout: 900
run:
  - python3 main.py > log-main.py.log
clean:
  - rm -rf *.trt *.log
