version: 1
name: 03-Workflow/pyTorch-TensorRT
tags:
  - workflow
  - pytorch
run:
  - "python3 main.py > log-main.py.log"
  - "pushd C++ && make test && popd"
clean:
  - "rm -rf *.trt* *.Int8Cache C++/*.d C++/*.o C++/*.exe C++/*.trt C++/*.Int8Cache *.log"
