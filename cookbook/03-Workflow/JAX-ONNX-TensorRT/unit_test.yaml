version: 1
name: 03-Workflow/JAX-ONNX-TensorRT
# Disabled: needs JAX, which is not in the base environment and is out of scope for the
# PyTorch-focused sweep.
enabled: false
tags:
  - workflow
  - jax
run:
  - python3 main.py > log-main.py.log
clean:
  - rm -rf *.log *.trt*
