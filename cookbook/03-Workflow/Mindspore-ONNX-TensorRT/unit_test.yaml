version: 1
name: 03-Workflow/Mindspore-ONNX-TensorRT
# Disabled: needs MindSpore, not in the base environment; out of scope for the PyTorch sweep.
enabled: false
tags:
  - workflow
  - mindspore
run:
  - python3 main.py > log-main.py.log
clean:
  - rm -rf *.log *.trt*
