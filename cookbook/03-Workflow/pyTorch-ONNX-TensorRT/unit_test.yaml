version: 1
name: 03-Workflow/pyTorch-ONNX-TensorRT
tags:
  - workflow
  - pytorch
  - onnx
run:
  - python3 main.py > log-main.py.log
  - cd C++ && make build && ./main.exe
clean:
  - rm -rf *.log *.trt* *.Int8Cache C++/*.log C++/*.trt* C++/*.Int8Cache
