version: 1
name: 03-Workflow/pyTorch-ONNX-TensorRT
tags:
  - workflow
  - pytorch
  - onnx
run:
  - "python3 main.py > log-main.py.log"
  - "pushd C++ && make -j && ./main.exe && popd"
clean:
  - "rm -rf *.trt* *.Int8Cache C++/*.d C++/*.o C++/*.exe C++/*.trt C++/*.Int8Cache *.log"
