version: 1
name: 03-Workflow/OneFlow-ONNX-TensorRT
# Disabled: needs OneFlow, not in the base environment; out of scope for the PyTorch sweep.
enabled: false
tags:
  - workflow
  - oneflow
run:
  - python3 main.py > log-main.py.log
clean:
  - rm -rf *.log *.trt*
