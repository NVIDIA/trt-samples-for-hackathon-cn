# 06-DLFrameworkTRT

+ TensorRT APIs in other Deep Learning Framework.

## ONNXRuntime-TensorRT

+ Use ONNX Runtime TensorRT Execution Provider (EP) for inference, and compare latency with CUDA EP.

## Paddle-TensorRT

+ Use Paddle Inference embedded TensorRT for inference, and compare latency with Paddle GPU predictor.

## TensorFlow-TensorRT

+ Train a TF2 CNN model on MNIST data from `00-Data`, then compare inference latency between:

## Torch-TensorRT

+ Use Torch-TensorRT (PyTorch 2.x Dynamo path) to compile a torch network and run it.
