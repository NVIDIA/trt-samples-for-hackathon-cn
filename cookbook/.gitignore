# common
__pycache__/
.claude/
.hypothesis/
.mypy_cache/
.pytest_cache/
.vscode/
*.gz
# *.log  # Publish log files to show output of the examples

# Private files
92-LocalFile/
99-Todo/README.md

# TensorRT
*.bin
*.d
*.engine
*.exe
*.Int8Cache
*.json
*.lock
*.o
*.raw
*.so
*.Tacitc
*.TimingCache
*.trt
*.trt*
*.plan
*.cache
*.wts

# Nsight Systems
*.nsys-rep
*.qdrep

# numpy
*.npy
*.npz

# Images generated by examples (03-Workflow/SemanticSegmentation-C++)
*.ppm

# ML framework
*.onnx
*.onnx.data
*.onnx.weight
*.pb
*.pkl
*.pth
*.safetensors
*.ts

# Downloaded checkpoints (see 00-Data/README.md). `*.json` above already covers config.json and
# the tokenizer json, but `merges.txt` / `vocab.txt` would otherwise be committable.
00-Data/model/gpt2/

# Packages
build/
dist/
tensorrt_cookbook.egg-info/

# Autotuner / runtime-cache output written by examples (see each `clean:` in unit_test.yaml)
07-Tool/QDQPlacementAutotune/out-*/
04-Feature/RefitObserver/*.txt

# Plan-file inspection depending on TensorRT's non-public layout; must not be published
tensorrt_cookbook/utils_private.py
