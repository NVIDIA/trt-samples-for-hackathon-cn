version: 1
name: 10-TensorRT-RTX
enabled: true
tags:
  - optional-package   # needs `pip install tensorrt_rtx`; skips cleanly without it
  - tensorrt-rtx
run:
  - python3 main.py     > log-main.py.log
  - python3 compare.py  > log-compare.py.log
clean:
  - rm -rf *.log *.trt
