version: 1
name: 10-TensorRT-RTX/03-RuntimeCache
enabled: true
tags:
  - optional-package   # needs `pip install tensorrt_rtx`; skips cleanly without it
  - tensorrt-rtx
  - runtime
  - runtimecache
run:
  - python3 main.py > log-main.py.log
clean:
  - rm -rf *.log *.trt
