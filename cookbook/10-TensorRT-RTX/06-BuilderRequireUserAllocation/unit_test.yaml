version: 1
name: 10-TensorRT-RTX/06-BuilderRequireUserAllocation
enabled: true
tags:
  - optional-package   # needs `pip install tensorrt_rtx`; skips cleanly without it
  - tensorrt-rtx
  - builder
  - userallocation
run:
  - python3 main.py > log-main.py.log
clean:
  - rm -rf *.log *.trt
