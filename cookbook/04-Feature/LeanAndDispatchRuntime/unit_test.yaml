version: 1
name: 04-Feature/LeanAndDispatchRuntime
enabled: true
tags:
  - optional-package   # needs `pip install tensorrt-lean tensorrt-dispatch`; skips cleanly without them
  - feature
  - leananddispatchruntime
run:
  - python3 main.py > log-main.py.log
clean:
  - rm -rf *.trt *.log
