# Strongly Typed

+ Usage of strongly-typed mode, which means all tensor data types are inferred from network inputs and operator type rules.

+ The recommended way to change dtype is to insert explicit conversion operators such as `Cast`.

+ Steps to run.

```bash
python3 main.py
```
